server.port=8080

# MongoDB Atlas connection
spring.data.mongodb.uri=mongodb+srv://ramudi:catpw189@cluster0.dik79.mongodb.net/smartcampus?retryWrites=true&w=majority

# H2 Database connection (for Booking/Resource models)
spring.datasource.url=jdbc:h2:mem:smartcampusdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT settings if needed (mock for now or based on teammate auth)
app.jwtSecret=SuperSecretKeyForSmartCampusProjectThatIsAtLeast32BytesLong
app.jwtExpirationMs=86400000

# CORS settings just in case
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
